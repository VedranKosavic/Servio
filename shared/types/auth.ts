/**
 * WP1 fills this in: `MeUser`, `DeviceBrief`, `MeContext`, `SessionBrief`
 * (`docs/BACKEND.md` §5.5). WP0 creates the file so the barrel compiles and the
 * types-only contract PR has somewhere to land.
 */
export {}
