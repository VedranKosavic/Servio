/**
 * WP1 fills this in (`docs/BACKEND.md` §5). WP0 creates the file so the barrel
 * in `shared/errors.ts` compiles from day one and WP1 never has to edit it.
 */
export const AUTH_ERRORS = {} as const
