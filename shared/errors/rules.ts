/**
 * WP0 owns this fragment: *Pravila* (PHASE4 §2.8).
 *
 * Two sentences, and both exist because the acknowledgement has to mean
 * something: it is a record that *this* text was read, so confirming an old
 * version is not confirming the current one.
 */
export const RULES_ERRORS = {
  RULES_STALE: 'Objavljena je novija verzija Pravila — otvori ih ponovo.',
  RULES_NOT_PUBLISHED: 'Pravila još nisu objavljena.',
}
